# The exact Chromium revision this series applies to. One line, nothing else.
#
# REPLACE THIS with the full 40-character revision on the first run from a real
# checkout — a shortened sha is not a pin, it is a prefix that will one day be
# ambiguous. `git -C <chromium>/src rev-parse HEAD` prints the right value.
bbbfd22
