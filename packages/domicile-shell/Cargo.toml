[package]
name = "domicile-shell"
description = "Resolving and starting a Domicile shell: the manifest a shell package declares, where a named one is looked up, and the process that runs it"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
domicile-config = { path = "../domicile-config" }
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
thiserror.workspace = true

[dev-dependencies]
domicile-protocol = { path = "../domicile-protocol" }
tempfile.workspace = true
