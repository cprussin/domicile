[package]
name = "domicile-bridge"
description = "Domicile AppTextureBridge: bookkeeping that maps app surfaces to engine external-image textures"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
thiserror.workspace = true

# A leftover of the embedding plan this crate was written for. Nothing enables
# it and nothing is behind it; it goes when the crate does.
[features]
default = []
cef = []
