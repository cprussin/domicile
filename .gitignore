# Rust
/target
**/target
Cargo.lock.orig

# Nix
result
result-*
.direnv

# Node / TypeScript
node_modules/
.turbo/
dist/
.vite/
out/
storybook-static/
styled-system/
*.tsbuildinfo
.env
.env.local
.env.*.local

# CEF binary distribution (large, fetched separately)
/vendor/cef
*.tar.bz2

# Editor / OS
*.swp
.DS_Store

# Local tool state
.claude/

# Python
#
# The guards' fixtures are scripts rather than a package, but importing one to
# check the pages it serves -- which is the only way to read them without
# starting a browser -- leaves bytecode beside it.
__pycache__
